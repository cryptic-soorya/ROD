# Core
fastapi==0.111.0
uvicorn==0.30.1
python-dotenv==1.0.1

# Auth
PyJWT==2.8.0
bcrypt==4.1.3

# MCP
fastmcp==0.4.1

# AI / Agent
anthropic==0.29.0
google-genai==2.10.0

# AI / Agent — LangGraph migration (in progress on branch `langg`, see PLAN.md)
langgraph==1.2.8
langchain-core==1.4.9
langchain-google-genai==4.2.7

# Knowledge Base (RAG) — Postgres/pgvector backed, shared across the team
# via the same Supabase DB every other domain uses (see
# knowledge_base/pg_vector_client.py). Embeddings still generated locally.
pgvector==0.5.0
sentence-transformers==3.0.1

# Postgres (was missing from this file despite db_pool.py/seeds/db.py
# depending on it since the SQLite->Postgres migration)
psycopg2-binary==2.9.12

# Data
pydantic>=2.7.4
#POSTGRE SQL
psycopg2-binary==2.9.10
# PDF export
reportlab==4.2.2

# Testing
pytest==8.2.2
httpx>=0.27.0
